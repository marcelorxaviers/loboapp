<%= render 'shared/contextheader' %>

<div class="page-header">
  <h1>Companies <small>Every existent company</small></h1>
</div>

<% if @companies.length == 0 %>
  <div class="well">
    <h3>Nothing here =(</h3>
    You may <%= link_to 'create new companies', new_company_path %>, as you wish
  </div>
<% else %>
  <%= render 'shared/companylisting' %>
<% end %>

